/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 */

'use strict'

const create = require('./create')
const PonLogger = require('./pon_logger')

let lib = create.bind(this)

Object.assign(lib, PonLogger, {
  create,
  PonLogger
})

module.exports = lib
